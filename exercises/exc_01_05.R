# Fill the following code to obtain a sequence of numbers from 5 to 20.

seq(from = _ _ _, to = _ _ _)

# Store the result of the function `seq(1, 10)` in an object with the assigment 
# arrow and multiply this object by 2:

my_seq <- seq(_ _ _, _ _ _)

_ _ _ * _ _ _

# Store the result of the function `seq(1, 10)` in an object with the assigment 
# arrow and store the value 2 in another object with the assigment arrow. Then 
# multiply these two objects:

my_seq <- seq(_ _ _, _ _ _)

val_two <- _ _ _

_ _ _ * _ _ _
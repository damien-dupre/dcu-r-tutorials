# install.packages("gapminder") has already been done, use the function library()
# to load the package:

library(_ _ _)

# Have a look at the `gapminder` object structure using the function `str()`:

str(_ _ _)

# Have a look at the first rows/observations of the `gapminder` object using the
# function `head()`:

head(_ _ _)

# Fill the following code to obtain a sequence of numbers from 5 to 20.

seq(from = 5, to = 20)

# Store the result of the function `seq(1, 10)` in an object with the assignment
# arrow and multiply this object by 2:

my_seq <- seq(1, 10)

my_seq * 2

# Store the result of the function `seq(1, 10)` in an object with the assignment
# arrow and store the value 2 in another object with the assignment arrow. Then
# multiply these two objects:

my_seq <- seq(1, 10)

val_two <- 2

my_seq * val_two
